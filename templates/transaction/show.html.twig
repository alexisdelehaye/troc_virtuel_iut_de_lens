{% extends 'base.html.twig' %}

{% block title %}Transaction{% endblock %}

{% block body %}
    <h1>Transaction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idtransaction</th>
                <td>{{ transaction.idtransaction }}</td>
            </tr>
            <tr>
                <th>Transactionrealisã©e</th>
                <td>{{ transaction.transactionrealisã©e ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('transaction_index') }}">back to list</a>

    <a href="{{ path('transaction_edit', {'idtransaction': transaction.idtransaction}) }}">edit</a>

    {{ include('transaction/_delete_form.html.twig') }}
{% endblock %}
