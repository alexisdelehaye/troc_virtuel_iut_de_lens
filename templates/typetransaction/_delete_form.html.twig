<form method="post" action="{{ path('typetransaction_delete', {'idtypetransaction': typetransaction.idtypetransaction}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ typetransaction.idtypetransaction) }}">
    <button class="btn">Delete</button>
</form>
