{% extends 'base.html.twig' %}

{% block title %}Profil{% endblock %}

{% block body %}
    <h1>Profil</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idprofil</th>
                <td>{{ profil.idprofil }}</td>
            </tr>
            <tr>
                <th>Nomprofil</th>
                <td>{{ profil.nomprofil }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('profil_index') }}">back to list</a>

    <a href="{{ path('profil_edit', {'idprofil': profil.idprofil}) }}">edit</a>

    {{ include('profil/_delete_form.html.twig') }}
{% endblock %}
